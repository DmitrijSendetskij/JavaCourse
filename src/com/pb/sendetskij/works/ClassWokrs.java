package com.pb.sendetskij.works;

import java.util.Scanner;

public class ClassWokrs {
    public static void main(String[] args) {
     //   Scanner in = new Scanner(System.in);
     //   System.out.println("Ведите");
     // x = scan.nextInt();
    // mathOper = scan.next();
    // try {} catch (Exception e) {}
    //   сравнить строки !login.equals(usersLogin) /  login.equals(usersLogin)
    /*
    Циклы
        while(условие){действие};
        while(условие){действие if(true){break завершить}};
        while(условие){действие if(true){continue.. начать сначала}};
        do{действие}while(условие);
     */
    /*
        Массивы
        Одномерные
        char Object String int double byte[] array = new byte[3]
        Класс Arrays.toString(array) - для одномерных
        Двуменрыне
        byte[]] array = new byte [2][2];
        Циклом вывести
        System.out.print("" + array[i][[j]);
        Arrays.deepToString(array)
        Сравнение Arrays.equals(a1,a2 zx xx)
     */
        /*
        Random random = new Random();
        int x = random.nextInt( bound: 101)
        double y Math.random(); 0 <1.0
        * */


        /*
        String a;


        String userLogin = in.next();
        if(!login.equals(usersLogin)){}
        */
        /*

        x = Integer.parseInt(s);
        */
    }
    // Методы и процедуры
    //static тип возвращаемых данных Название(Водящие параметры если есть)
    // void / string / int
    // function,procedure(Параметры на вод тип+Название через запятую)
    // int blabla = Integer.parseInt(параметр) конвертация в инт
    //
    //java/lang/SttringBuilder
    // StringBuilder  buf = new StringBuilder()
    // buf.append("")
    //String asd  = new StringBuilder().append("").append("").toString();
    // String DDD = new StringBuffer() - потокобезопасная
    // Arrays.toString()
    // "dfsdfsd".toCharArray()

}
